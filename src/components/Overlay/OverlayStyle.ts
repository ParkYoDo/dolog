import styled from 'styled-components';
import { LayoutWrap } from '@styles/GlobalStyle';

export const OverlayWrap = styled(LayoutWrap)`
  position: fixed;
  top: 0;
  left: 0;
  background-color: rgba(0, 0, 0, 0.5);
`;
